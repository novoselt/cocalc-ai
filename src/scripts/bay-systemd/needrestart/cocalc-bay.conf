# CoCalc bay services require controlled drain and restart orchestration.
# Unattended security updates may be installed, but needrestart must not
# restart the bay database or control plane behind the orchestrator's back.
$nrconf{override_rc}{qr(^cocalc-bay-.*\.service$)} = 0;
