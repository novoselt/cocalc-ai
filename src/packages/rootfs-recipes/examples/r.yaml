version: 1
name: r

steps:
  - uses: cocalc/r
    timeout: 1800
  - uses: cocalc/rstudio
    timeout: 1800

verify:
  - command -v R
  - command -v jupyter
  - command -v jupyter-lab
  - command -v rserver
  - python3 -c 'import ipykernel, jupyterlab'
  - Rscript --vanilla -e 'stopifnot(requireNamespace("IRkernel", quietly=TRUE))'
  - Rscript --vanilla -e 'stopifnot(requireNamespace("shiny", quietly=TRUE))'
  - jupyter kernelspec list | grep -qi ir

publish:
  label: R
  slug: r
  description: R with common development libraries, JupyterLab, an IRkernel Jupyter kernel, RStudio Server, and a Shiny example app.
  family: r
  version: stable
  channel: stable
  visibility: public
  tags:
    - r
    - statistics
    - jupyter
    - rstudio
    - shiny
